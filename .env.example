# Larpwing router API key — clients send this as Bearer token
# Generate one with: python configure.py
ROUTER_API_KEY=*** ── Provider API keys ───────────────────────────────────────────────
# Run `python configure.py` to add provider keys interactively.
# They will appear here automatically as ALIAS_API_KEY and ALIAS_API_URL.
